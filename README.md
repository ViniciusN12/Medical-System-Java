# 🏥 Sistema Médico Web

Sistema web para gestão de pacientes desenvolvido com **Spring Boot**, **Thymeleaf** e **Oracle Database**.

![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-4.0.1-brightgreen)
![Oracle](https://img.shields.io/badge/Oracle-XE-red)
![License](https://img.shields.io/badge/License-MIT-blue)

## 📋 Funcionalidades

- ✅ Cadastro de pacientes
- ✅ Listagem de pacientes
- ✅ Edição de pacientes
- ✅ Exclusão de pacientes
- ✅ Gerenciamento de endereços

## 🛠️ Tecnologias Utilizadas

| Tecnologia | Versão |
|------------|--------|
| Java | 21 |
| Spring Boot | 4.0.1 |
| Thymeleaf | 3.1.3 |
| Oracle Database | XE |
| Tailwind CSS | 3.x (CDN) |
| Font Awesome | 6.0 |

## 📁 Estrutura do Projeto

```
sistema-medico-web/
├── src/main/java/com/vinicius/sistema_medico_web/
│   ├── controller/
│   │   └── PacienteController.java
│   ├── dao/
│   │   ├── EnderecoDAO.java
│   │   └── PacienteDAO.java
│   ├── model/
│   │   ├── Endereco.java
│   │   └── Paciente.java
│   └── SistemaMedicoWebApplication.java
├── src/main/resources/
│   ├── templates/
│   │   ├── index.html
│   │   └── pacientes/
│   │       ├── novo-paciente.html
│   │       └── editar-paciente.html
│   └── application.properties
└── pom.xml
```

## ⚙️ Pré-requisitos

- **Java 21** ou superior
- **Oracle Database XE** (ou outra versão compatível)
- **Maven** (ou use o wrapper incluso)

## 🗃️ Configuração do Banco de Dados

Execute os seguintes scripts SQL no Oracle:

```sql
-- Criar tabela de endereços
CREATE TABLE Endereco (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    logradouro VARCHAR2(100) NOT NULL,
    numero VARCHAR2(10) NOT NULL,
    complemento VARCHAR2(50),
    bairro VARCHAR2(50) NOT NULL,
    cidade VARCHAR2(50) NOT NULL,
    uf VARCHAR2(2) NOT NULL,
    cep VARCHAR2(10) NOT NULL
);

-- Criar tabela de pacientes
CREATE TABLE Paciente (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR2(100) NOT NULL,
    cpf VARCHAR2(11) UNIQUE NOT NULL,
    telefone VARCHAR2(15),
    endereco_id NUMBER,
    data_nascimento DATE NOT NULL,
    FOREIGN KEY (endereco_id) REFERENCES Endereco(id)
);
```

## 🔧 Configuração

1. Clone o repositório:
```bash
git clone https://github.com/ViniciusN12/Medical-System-Java.git
cd Medical-System-Java
```

2. Configure a conexão com o banco de dados em `PacienteDAO.java` e `EnderecoDAO.java`:
```java
private static final String URL = "jdbc:oracle:thin:@localhost:1521:xe";
private static final String USER = "seu_usuario";
private static final String PASSWORD = "sua_senha";
```

## 🚀 Como Executar

### Via Maven Wrapper (recomendado)

```bash
# Windows
.\mvnw.cmd spring-boot:run

# Linux/Mac
./mvnw spring-boot:run
```

### Via IDE

1. Importe o projeto como Maven Project
2. Execute a classe `SistemaMedicoWebApplication.java`

### Acesse a aplicação

Abra o navegador e acesse: **http://localhost:8080**

## 📸 Screenshots

### Página Inicial - Lista de Pacientes
Interface com tabela listando todos os pacientes cadastrados, com opções de editar e excluir.

### Cadastro de Paciente
Formulário completo para cadastro de novos pacientes com dados pessoais e endereço.

### Edição de Paciente
Formulário para atualização dos dados do paciente.

## 🔗 Endpoints

| Método | Rota | Descrição |
|--------|------|-----------|
| GET | `/` | Lista todos os pacientes |
| GET | `/novo-paciente` | Formulário de cadastro |
| POST | `/salvar-paciente` | Salva novo paciente |
| GET | `/editar-paciente?id={id}` | Formulário de edição |
| POST | `/atualizar-paciente` | Atualiza paciente |
| GET | `/deletar-paciente?id={id}` | Remove paciente |

## 👨‍💻 Autor

**Vinicius** - [GitHub](https://github.com/ViniciusN12)

## 📄 Licença

Este projeto está sob a licença MIT. Veja o arquivo [LICENSE](LICENSE) para mais detalhes.
